rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // PROTOTYPE MODE: Allow reading and writing for the dashboard to function 
      // with custom login. In production, use Firebase Authentication.
      allow read, write: if true;
    }
  }
}
